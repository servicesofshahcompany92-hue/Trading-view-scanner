name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libffi-dev libssl-dev libgdbm-dev libsqlite3-dev libreadline-dev libbz2-dev ccache

    - name: Install Buildozer & Cython
      run: |
        pip install --upgrade pip
        pip install --upgrade buildozer cython==0.29.33

    - name: Build APK
      run: |
        buildozer android debug

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: TradingScannerApp
        path: bin/*.apk
